---
- name: Install Softwares
  hosts: all
  become: true
  become_user: Administrator
  become_method: runas
  tasks:
    # This assumed chocolatey is already installed. If not then install chocolatey first
    - name: Install softwares using chocolatey
      win_chocolatey:
        name: "{{ item }}"
        state: present
        force: true
      loop:
        - 7zip
        - awscli
        - dotnet-9.0-runtime
        - git
        - golang
        - nodejs-lts
        - notepadplusplus
        - python313
        - ruby
        - vscode

