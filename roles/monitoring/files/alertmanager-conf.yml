global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'email-notifications'

receivers:
- name: 'email-notifications'
  email_configs:
  - to: 'admin@superasian.net'
    from: 'k0ng@superasian.net'
    smarthost: smtp.superasian.net:587
    auth_username: 'guest'
    auth_password: 'guest'